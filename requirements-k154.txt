# Exact versions from the qualified CUDA 13 / PyTorch 2.13.0 container.
# Install torch from the CUDA 13 index first; then install this file.
transformers==5.17.0
tokenizers==0.23.2
safetensors==0.8.0
numpy==2.3.5
sympy==1.14.0
